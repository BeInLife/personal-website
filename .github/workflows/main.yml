name: Standard workflow
on: push

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Install deps
        run: yarn install
      - name: Lint
        run: yarn lint
      - name: Build
        run: yarn build
